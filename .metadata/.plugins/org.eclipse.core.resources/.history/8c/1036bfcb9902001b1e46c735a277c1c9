/*Autor: José Rodrigo Mejía Velázquez
 *Fecha: 29/09/2020
 *Descripción: Clase Pila de proyecto Pilas 3, contiene los atributos stk (pila) y tope, así como
 *			   los contructores, uno por default que no recibe parámetros y otro que recibe un valor
 *			   de tipo de entero para establecer el tamaño de pila, además de los métodos push y pop. 
*/

package Pilas3;

public class Pila {
	private int[] stk;
	private int tope =-1;
	
	Pila() {
		stk = new int[3];
	}
	
	Pila(int tampila) {
		stk = new int[tampila];
	}
	
	void push(int num) {
		
		if(tope == (stk.length)-1) {
			System.out.println("La Pila está llena");
		} else {
			tope++;
			stk[tope] = num;
		}
	}
	
	Integer pop() {
		Integer dato = null;
		if(tope == -1) {
			System.out.println("La Pila está vacia");
		} else {
			dato = stk[tope];
			tope--;
		}
		return dato;
	}
	
/*	void muestraDatos() {	//Método funcional, pero del que no se esta haciendo uso.
		if(tope == -1) {
			System.out.println("La pila está vacia");
		}else {
			System.out.println("Los valores en la pila son: ");
			for(int i = 0; i<=tope;i++)
				System.out.println(stk[i]);
		}
	}
	
	int getTamP() {
		return stk.length;
	}
							//Getters que no se utilizan
	int getNumDatos() {
		return tope+1;
	}
*/
}
